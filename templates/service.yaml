apiVersion: v1
kind: Service
metadata:
  name: {{ include "php-app.fullname" . }}
  labels:
    app: {{ include "php-app.name" . }}
spec:
  type: NodePort
  selector:
    app: {{ include "php-app.name" . }}
  ports:
    - port: 80
      targetPort: 80
